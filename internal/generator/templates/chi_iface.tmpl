// Code generated by go-mir. DO NOT EDIT.
// versions:
// - mir {{ .VerInfo.MirVer }}

package {{ .PkgName }}

import (
    "net/http"

    "github.com/alimy/mir/v4"
    {{if notEmptyStr .EngineInfo.PkgName }}chi "{{ .EngineInfo.PkgName }}{{else}}"github.com/go-chi/chi/v5{{end}}"
	{{ declareImports .Imports }}
)
{{- if .DeclareCoreInterface }}
type _binding_ interface {
	Bind(*http.Request) mir.Error
}

type _render_ interface {
	Render(http.ResponseWriter)
}

type _default_ interface {
	Bind(*http.Request, any) mir.Error
	Render(http.ResponseWriter, any, mir.Error)
}
{{- end }}

{{ declareTypes .InOuts .PkgPath .Imports }}

{{if notEmptyStr .Comment }}// {{.Comment}}{{end}}
type {{.TypeName}} interface {
    _default_

{{if notEmptyStr .Chain }}    // Chain provide middlewares for chi
    {{.Chain}}() chi.Middlewares
{{end}}
{{range .Fields}}    {{.MethodName}}({{if notEmptyStr .InName }}*{{ .InName }}{{end}}) {{if notEmptyStr .OutName }}(*{{ .OutName}}, mir.Error){{else}}mir.Error{{end}}
{{end}}

    mustEmbedUnimplemented{{.TypeName}}Servant()
}
{{if .IsUseFieldChain }}
type {{.TypeName}}Chain interface {
{{range .ChainFields }}    Chain{{.MethodName}}() chi.Middlewares
{{end}}

	mustEmbedUnimplemented{{.TypeName}}Chain()
}
{{end}}

// Register{{.TypeName}}Servant register {{.TypeName}} servant to chi
func Register{{.TypeName}}Servant(e chi.Router, s {{.TypeName}}{{if .IsUseFieldChain }},  m ...{{.TypeName}}Chain{{end}}) {
{{- if .IsUseFieldChain -}}
	var cc {{.TypeName}}Chain
	if len(m) > 0 {
		cc = m[0]
	} else {
		cc = &Unimplemented{{.TypeName}}Chain{}
	}
{{- end}}
{{if notEmptyStr .Group }}    router := chi.NewRouter()
    e.Mount("{{.Group}}", router){{else}}    router := e{{end}}
{{if notEmptyStr .Chain }}
    // use chain for router
    middlewares := s.{{.Chain}}()
    router.Use(middlewares...)
{{end}}
    // register routes info to router
{{range .Fields}}{{if .NotHttpAny }}     {{if .IsFieldChain }}router.With(cc.Chain{{.MethodName}}()...){{else}}router{{end}}.MethodFunc("{{.HttpMethod}}", "{{.Path}}", func(rw http.ResponseWriter, hr *http.Request) {
	{{- if $.WatchCtxDone }}		
		select {
		case <- hr.Context().Done():
			return
		default:
		}
	{{end -}}
	{{if notEmptyStr .InName -}}
	req := new({{.InName}})
	{{if .IsBindIn -}}
	var bv _binding_ = req
	if err := bv.Bind(hr); err != nil {
	{{- else -}}
	if err := s.Bind(hr, req); err != nil {
	{{- end }}
		s.Render(rw, nil, err)
		return
	}
	{{- end }}
	{{if notEmptyStr .OutName -}}
	resp, err := s.{{ .MethodName}}({{if notEmptyStr .InName}}req{{end}})
	{{if .IsRenderOut -}}
	if err != nil {
		s.Render(rw, nil, err)
		return
	}
	var rv _render_ = resp
	rv.Render(rw)
	{{- else -}}
	s.Render(rw, resp, err)
	{{- end }}
	{{- else -}}
	s.Render(rw, nil, s.{{.MethodName}}({{if notEmptyStr .InName}}req{{end}}))
	{{- end }}
	})
	{{else if .JustHttpAny}}    {{if .IsFieldChain }}router.With(cc.Chain{{.MethodName}}()...){{else}}router{{end}}.HandleFunc("{{.Path}}", func(rw http.ResponseWriter, hr *http.Request) {
	{{- if $.WatchCtxDone }}		
		select {
		case <- hr.Context().Done():
			return
		default:
		}
	{{end -}}
	{{if notEmptyStr .InName -}}
	req := new({{.InName}})
	{{if .IsBindIn -}}
	var bv _binding_ = req
	if err := bv.Bind(hr); err != nil {
	{{- else -}}
	if err := s.Bind(hr, req); err != nil {
	{{- end }}
		s.Render(rw, nil, err)
		return
	}
	{{- end }}
	{{if notEmptyStr .OutName -}}
	resp, err := s.{{ .MethodName}}({{if notEmptyStr .InName}}req{{end}})
	{{if .IsRenderOut -}}
	if err != nil {
		s.Render(rw, nil, err)
		return
	}
	var rv _render_ = resp
	rv.Render(rw)
	{{- else -}}
	s.Render(rw, resp, err)
	{{- end }}
	{{- else -}}
	s.Render(rw, nil, s.{{.MethodName}}({{if notEmptyStr .InName}}req{{end}}))
	{{- end }}
	})
	{{else}}{{$field := .}}    {
        	h := func(rw http.ResponseWriter, hr *http.Request) {
	{{- if $.WatchCtxDone }}		
		select {
		case <- hr.Context().Done():
			return
		default:
		}
	{{end -}}
	{{if notEmptyStr .InName -}}
	req := new({{.InName}})
	{{if .IsBindIn -}}
	var bv _binding_ = req
	if err := bv.Bind(hr); err != nil {
	{{- else -}}
	if err := s.Bind(hr, req); err != nil {
	{{- end }}
		s.Render(rw, nil, err)
		return
	}
	{{- end }}
	{{if notEmptyStr .OutName -}}
	resp, err := s.{{ .MethodName}}({{if notEmptyStr .InName}}req{{end}})
	{{if .IsRenderOut -}}
	if err != nil {
		s.Render(rw, nil, err)
		return
	}
	var rv _render_ = resp
	rv.Render(rw)
	{{- else -}}
	s.Render(rw, resp, err)
	{{- end }}
	{{- else -}}
	s.Render(rw, nil, s.{{.MethodName}}({{if notEmptyStr .InName}}req{{end}}))
	{{- end }}
	    }
		{{if .IsFieldChain }}        router := router.With(cc.Chain{{.MethodName}}()...){{end}}
		{{range .AnyHttpMethods}}        router.MethodFunc("{{.}}", "{{$field.Path}}", h)
		{{end}}    }
	{{end}}
{{- end -}}
}

{{ $unimplementedServant := print "Unimplemented" .TypeName "Servant" }}
// {{$unimplementedServant}} can be embedded to have forward compatible implementations.
type {{$unimplementedServant}} struct {}

{{if notEmptyStr .Chain }}
func ({{$unimplementedServant}}){{.Chain}}() chi.Middlewares {
    return nil
}

{{end}}
{{range .Fields}}
func ({{$unimplementedServant}}){{.MethodName}}({{if notEmptyStr .InName }}req *{{ .InName }}{{end}}) {{if notEmptyStr .OutName }}(*{{ .OutName}}, mir.Error){{else}}mir.Error{{end}} {
	return {{if notEmptyStr .OutName }}nil, {{end}}mir.Errorln(http.StatusNotImplemented, http.StatusText(http.StatusNotImplemented))
}

{{end}}
func ({{$unimplementedServant}})mustEmbedUnimplemented{{.TypeName}}Servant() {}
{{if .IsUseFieldChain }}
{{ $unimplementedChain := print "Unimplemented" .TypeName "Chain" }}
// {{$unimplementedChain}} can be embedded to have forward compatible implementations.
type {{$unimplementedChain}} struct {}

{{range .Fields}}{{if .IsFieldChain }}func (b *{{$unimplementedChain}}) Chain{{.MethodName}}() chi.Middlewares {
	return nil
}
{{end}}
{{end}}
func (b *{{$unimplementedChain}})mustEmbedUnimplemented{{.TypeName}}Chain() {}
{{end}}
