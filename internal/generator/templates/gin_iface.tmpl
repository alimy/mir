// Code generated by go-mir. DO NOT EDIT.

package {{ .PkgName }}

import (
    "github.com/gin-gonic/gin"
)

{{if notEmptyStr .Comment }}
// {{.Comment}}
{{end}}
type {{.TypeName}} interface {
{{if .IsNeedChain}}
    // Chain provider handlers chain for gin
    Chain() gin.HandlersChain
{{end}}
{{range .Fields}}
    {{if NotEmpty .Comment }}
    // {{.Comment}}
    {{end}}
    {{.MethodName}}(c *gin.Context)
{{end}}
}

// Register{{.TypeName}}Servant register site to gin
func Register{{.TypeName}}Servant(e *gin.Engine, s {{.TypeName}}) {
    router := e.Group({{.Group}})
{{if .IsNeedChain}}
    // use chain for router
    handlersChain := s.Chain()
    router.Use(handlersChain...)
{{end}}

    // register routes info to router
{{range .Fields}}
    {{if notHttpAny .HttpMethod }}
    router.Handle({{.HttpMethod}}, {{.Uri}}, s.{{.MethodName}})
    {{else}}
    router.Any({{.Uri}}, s.{{.MethodName}})
    {{end}}
{{end}}
}
